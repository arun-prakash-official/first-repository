class Solution:
    def evenlyDivides(self, n):
        string = str(n)
        result = 0
        for i in string:
            if int(i) != 0 and n % int(i) == 0:
                result +=1
        return result
